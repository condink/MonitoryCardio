<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tcc.monitorycardio.com.monitorycardio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

            <activity android:name=".Activity.CadastroUsuario" />
            <activity android:name=".Activity.CadastroFilho" />
            <activity android:name=".Activity.IncluirDispositivo" />
            <activity android:name=".Activity.DefinirRelatorio" />
            <activity android:name=".Activity.CadastrarDependente" />
        <!--<activity android:name=".Activity.MainActivity" />-->

    </application>

</manifest>